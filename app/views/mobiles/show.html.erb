<!DOCTYPE html>
<html>

<head>

  <title>Mobile Shop</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>


  <style>
      body {
          font-family: 'Roboto', sans-serif;
          font-weight: 300;
      }

      header {
          padding: 20px 0;
      }

      header .row,
      footer .row {
          display: flex;
          align-items: center;
      }

      header h1 {
          font-weight: 700;
          margin: 0;
      }

      header nav {
          display: flex;
          justify-content: flex-end;
      }

      header p {
          padding: 0 20px;
          margin: 0;
      }


      .card {
          color: black;
          display: block;
          margin: 4px 0;

      }

      .tags .card h2 {
          color: black;
          position: absolute;
          left: 0;
          right: 0;
          text-align: center;
          top: 25%;
      }

      .tags img,
      .tag img {
          display:block;
          width:100%;

          height: 100%;
      }

      .tag h2, .tag .card h2, .destination h2, .destination p {
          padding: 0 20px 0;
          display: block;
      }

      .tag .card p {
          color: #0f0f0f;
          font-size: 14px;
          font-weight: 300;
          -webkit-box-orient: vertical;
          padding: 0 20px 20px;
      }


  </style>
</head>

<body>

<header class="containter">

  <div class="row">
    <h1 class="col-xs-4">Mobile Shop</h1>
    <nav class="col-xs-8 text-right">
      <% if !current_user %>
          <p><%= link_to "Početna", '/' %></p>
          <p><%= link_to "LogIn", '/login' %></p>
          <p><%= link_to "SignUp", '/signup' %></p>


      <% else %>
          <p><%= current_user.email %></p>
          <p><%= link_to "Početna", '/' %></p>
          <p><%= link_to "Logout", logout_path, method: "delete" %> </p>

      <% end %>
      <% if current_user && current_user.admin? %>
          <p><%= link_to "Poruke", '/orders/index' %></p>
          <p><%= link_to "Dodaj", '/mobiles/new' %></p>
      <% end %>
      <% if current_user && current_user.user? %>
          <p><%= link_to "Kontakt", '/orders/new' %></p>
      <% end %>
    </nav>
  </div>

</header>


<div class="tag">
  <div class="container">
    <h2><%= @mobile.ime %> <%= @mobile.opis %></h2>

    <div class="cards row">

      <div class="card col-xs-12">
        <%= image_tag @mobile.slika %>

        <p><%= @mobile.specifikacije %></p>

        <% if current_user && current_user.admin? %>
        <%= button_to "Ažuriraj", edit_path_path(@mobile), method: :get, class: "btn btn-primary btn-block"  %> <br/>

                    <p>  <%= link_to "Brisanje",delete_path_path, method: "delete"  %>   </p>
                               <% end %>
            </div>

            </div>

            </div>
            </div>


            </body>


            </html>


